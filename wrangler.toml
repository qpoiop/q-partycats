name = "partycats"
compatibility_date = "2024-12-01"

# Serve the Vite build output as static assets from a Cloudflare Worker.
# Unused / oversized source models are excluded via public/.assetsignore.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
